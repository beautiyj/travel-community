<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gnagnoohc.travel.community.mapper.CommunityMapper">
	<!-- 목록: 검색어(q)와 카테고리로 동적 필터 (selectAll/countAll 공용)
	     searchType: title(제목) / writer(작성자) / content(내용). 미지정(기본 "전체")이면 제목+작성자 OR 검색 유지 -->
	<sql id="listFilter">
    	<where>
        <if test="category != null and category != '' and category != '전체'">
            and p.category = #{category}
        </if>
        <if test="q != null and q != ''">
            <choose>
                <when test="searchType == 'title'">
                    and p.title like concat('%', #{q}, '%')
                </when>
                <when test="searchType == 'writer'">
                    and m.nickname like concat('%', #{q}, '%')
                </when>
                <when test="searchType == 'content'">
                    and p.content like concat('%', #{q}, '%')
                </when>
                <otherwise>
                    and (p.title like concat('%', #{q}, '%')
                         or m.nickname like concat('%', #{q}, '%'))
                </otherwise>
            </choose>
        </if>
    </where>
	</sql>

	<select id="selectAll" resultType="community">
    	select p.post_id, p.member_id, p.place_id, p.title, p.content,
        p.created_at, p.updated_at, p.readcount, p.category, m.nickname as nickname
		from post p
		join member m on p.member_id = m.member_id
    	<include refid="listFilter" />
    	order by p.post_id desc
    	limit #{limit} offset #{offset}
	</select>

	<!-- 목록 페이지네이션용 총 개수 (selectAll과 동일한 필터 조건 재사용) -->
	<select id="countAll" resultType="int">
		select count(*)
		from post p
		join member m on p.member_id = m.member_id
		<include refid="listFilter" />
	</select>

	<!-- 단건 조회 (place 테이블 LEFT JOIN: place_id가 NULL인 글도 조회되어야 하므로 LEFT JOIN 사용)
	     ※ place 상호명 컬럼은 name (title -> name 으로 컬럼명 변경됨) -->
	<select id="selectOne" parameterType="int" resultType="community">
		select p.post_id, p.member_id, p.place_id, p.title, p.content, p.created_at, p.updated_at, p.readcount,
		p.category, m.nickname as nickname, m.member_type as memberType, m.profile_img_url as profileImgUrl,
		biz.first_image as businessImage, pl.name as placeName, pl.place_type as placeType
		from post p
		join member m on p.member_id = m.member_id
		left join place pl on p.place_id = pl.place_id
		left join place biz on biz.member_id = p.member_id
		where p.post_id = #{postId}
	</select>
	
	<!-- 조회수 증가 -->
	<update id="updateReadcount" parameterType="int"> 
		update post set readcount = readcount + 1 where post_id = #{postId} 
	</update>
	
	<!-- 등록 (post_id 자동 생성값을 dto.postId 로 반환) -->
	<!-- nickname은 post에 저장하지 않음 (member_id만 저장) -->
	<insert id="insert" parameterType="community" useGeneratedKeys="true" keyProperty="postId"> 
		insert into post (member_id, place_id, title, content, category) 
		values (#{memberId}, #{placeId}, #{title}, #{content}, #{category}) 
	</insert>
	
	<!-- 수정 (place_id 포함: 방문자인증후기가 아니면 컨트롤러에서 미리 null 처리해서 넘겨줌) -->
	<update id="update" parameterType="community"> update post set title=
		#{title}, content= #{content}, category = #{category}, place_id = #{placeId} where post_id = #{postId} 
	</update>
	
	<!-- 삭제 -->
	<delete id="delete" parameterType="int">
		delete from post where post_id = #{postId}
	</delete>

	<!-- 일반후기 다중 장소 태그 (post_place_tag). placeIds가 비어있으면 컨트롤러에서 아예 호출하지 않음 -->
	<insert id="insertPlaceTags">
		insert into post_place_tag (post_id, place_id)
		values
		<foreach collection="placeIds" item="placeId" separator=",">
			(#{postId}, #{placeId})
		</foreach>
	</insert>

	<!-- 수정 시 전체 교체 방식으로 기존 태그를 먼저 지움 -->
	<delete id="deletePlaceTags" parameterType="int">
		delete from post_place_tag where post_id = #{postId}
	</delete>

	<!-- 일반후기 상세/수정 화면에 표시할 태그된 장소 목록 -->
	<select id="selectPlaceTags" parameterType="int" resultType="com.gnagnoohc.travel.community.dto.PlaceTagDto">
		select pl.place_id as placeId, pl.name as name, pl.place_type as placeType
		from post_place_tag ppt
		join place pl on ppt.place_id = pl.place_id
		where ppt.post_id = #{postId}
		order by pl.name asc
	</select>
</mapper>