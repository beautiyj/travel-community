<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gnagnoohc.travel.mypage.repository.MypageRepository">

    <!-- 회원 정보 조회 -->
    <select id="getMemberInfo"
            parameterType="long"
            resultType="com.gnagnoohc.travel.mypage.dto.MypageDto">

        select member_id, login_id, name, nickname, phone, gender, 
        birth, profile_img_url, signup_type, created_at, updated_at
        from member
        where member_id = #{memberId}

    </select>

    <!-- 회원 정보 수정 -->
    <update id="updateMember"
            parameterType="com.gnagnoohc.travel.mypage.dto.MypageDto">

        update member
        set
            name = #{name},
            nickname = #{nickname},
            phone = #{phone}
        where member_id = #{memberId}

    </update>

    <!-- 비밀번호 변경 -->
    <update id="changePassword"
            parameterType="com.gnagnoohc.travel.mypage.dto.MypageDto">

        update member
        set password = #{newPassword}
        where member_id = #{memberId}

    </update>

    <!-- 찜 목록 조회 -->
    <select id="getWishlist"
            parameterType="long"
            resultType="com.gnagnoohc.travel.mypage.dto.MypageDto">

        select
            wishlist_id,
            member_id,
            place_id,
            created_at
        from wishlist
        where member_id = #{memberId}
        order by created_at desc

    </select>

<update id="withdrawMember" parameterType="long">
    update member
    set member_status = 'WITHDRAWN',
        deleted_at = now(),
        updated_at = now()
    where member_id = #{memberId}
</update>


</mapper>